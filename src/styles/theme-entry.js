/* Generic Theme Style entry
 *
 * This combines ALL styles and provides NO pagespeed benefit.
 * This is only used as a fallback for any pages that don't have
 * their own entry style.
 *
 */

import 'Styles/theme.scss';
